{
    "layer": "top",
    "position": "top",
    "height": 25,

    "modules-center": ["clock", "memory", "cpu"],

    "clock": {
        "format": "  {:%Y-%m-%d %a %H:%M}"
    },

    "memory": {
        "format": "  {used}G"
    },

    "cpu": {
        "format": "  {usage}%"
    }
}
